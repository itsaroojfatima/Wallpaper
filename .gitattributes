# Auto detect text files and perform LF normalization
* text=auto eol=lf

# Windows batch files must use CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# Bash / shell scripts must use LF
*.sh text eol=lf
gradlew text eol=lf
